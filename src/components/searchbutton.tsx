import { Button } from "@/components/ui/button";

export function SearchButton() {
    return (
        <Button style={{
            backgroundColor: 'black',
            color: 'white',
            borderColor: 'white',
            borderWidth: '1px',
            borderStyle: 'solid'
        }}>Create Feed</Button>
    );
}